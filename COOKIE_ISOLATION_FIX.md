# Исправление изоляции cookies между фронтендами

## 🔍 Проблема

При входе в админку на `core.lead-schem.ru`, а затем переходе в панель директора `new.lead-schem.ru`, куки администратора использовались для авторизации директора. Это приводило к отображению данных всех городов вместо только своего города.

**Причина:** Все куки устанавливались с `domain: '.lead-schem.ru'`, что делало их доступными для всех поддоменов.

## ✅ Решение

Реализована **динамическая изоляция cookies** на основе origin запроса:

### 1. **Уникальные имена cookies** для каждого фронтенда:
- `lead-schem.ru` (основной домен) → `access_token_masters`, `refresh_token_masters`
- `core.lead-schem.ru` → `access_token_core`, `refresh_token_core`
- `new.lead-schem.ru` → `access_token_new`, `refresh_token_new`
- `callcentre.lead-schem.ru` → `access_token_callcentre`, `refresh_token_callcentre`
- `api.lead-schem.ru` → `access_token_api`, `refresh_token_api`

### 2. **Динамическое определение домена** на основе origin:
- Все остаются с `domain: '.lead-schem.ru'` для работы с API
- Но благодаря разным именам cookies - они изолированы

### 3. **Обратная совместимость**:
- Guard проверяет сначала динамическое имя cookie
- Fallback на базовое имя `access_token` для старых клиентов
- JSON авторизация продолжает работать без изменений

## 📝 Изменённые файлы

### `src/config/cookie.config.ts`
- ✅ Убрана жестко заданная настройка `domain: '.lead-schem.ru'`
- ✅ Добавлена функция `getCookieOptions(origin, maxAge)` - динамическое определение настроек
- ✅ Добавлена функция `getCookieName(baseName, origin)` - уникальные имена cookies

### `src/modules/auth/auth.controller.ts`
- ✅ Метод `login()` - использует динамические имена и настройки cookies
- ✅ Метод `refresh()` - поддержка динамических имен при чтении и записи
- ✅ Метод `logout()` - очистка правильных cookies на основе origin

### `src/modules/auth/guards/cookie-jwt-auth.guard.ts`
- ✅ Извлечение токена с учётом динамического имени cookie
- ✅ Fallback на базовое имя для обратной совместимости

## 🎯 Результат

Теперь куки изолированы между фронтендами:
- ✅ Вход в `core.lead-schem.ru` → куки только для админки
- ✅ Вход в `new.lead-schem.ru` → куки только для директора
- ✅ Нет конфликтов между разными ролями и доменами
- ✅ Каждый фронтенд получает только свои данные

## 🔄 Миграция

Изменения применяются автоматически:
1. При следующем логине пользователь получит cookie с новым именем
2. Старые cookies (`access_token`) перестанут использоваться
3. Новые cookies (`access_token_core`, `access_token_new`) изолированы

## 🧪 Тестирование

1. Войти в админку на `core.lead-schem.ru`
2. Проверить DevTools → Application → Cookies: должен быть `access_token_core`
3. Открыть `new.lead-schem.ru` в той же сессии
4. Войти как директор
5. Проверить DevTools → Cookies: должен быть `access_token_new`
6. Убедиться, что директор видит только свои данные

## 📌 Важно

- Все фронтенды должны отправлять header `x-use-cookies: true` для использования cookie-авторизации
- Origin должен быть правильно настроен в CORS
- Для production необходимо `secure: true` (HTTPS)

---

**Дата:** 2025-01-25
**Автор:** AI Assistant

